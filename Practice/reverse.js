// reverse() method

// using the reverse() method you can reverse an array.

const numbers = [2, 5, 4, 6, 7, 3];

numbers.reverse();
console.log(numbers);   // [3, 7, 6, 4, 5, 2]


const str = 'Coding is fun!';

const res = str.split('').reverse().join('');

console.log(res);