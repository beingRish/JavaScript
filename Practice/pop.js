// pop() method 

// pop() method remove the last item from the array and it will return it

const numbers = [1, 2, 3, 4, 5]

const lastItem = numbers.pop();

console.log(lastItem); // 5
console.log(numbers); // [1, 2, 3, 4]
